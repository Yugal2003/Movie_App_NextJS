// next.config.mjs
const nextConfig = {
    images: {
      domains: ['image.tmdb.org'], // Whitelist the domain
    },
    reactStrictMode: true,
  };
  
  // Export the configuration using ESM syntax
  export default nextConfig;
  